function User(emirim_id, password, name, room_num, parent_phone) {
    this.emirim_id = emirim_id;
    this.password = password;
    this.name = name;
    this.room_num = room_num;
    this.parent_phone = parent_phone;
};